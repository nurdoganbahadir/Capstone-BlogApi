{
  "name": "fullstack-project-capstoneblogapi",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run start:backend\"",
    "start:backend": "npm --prefix backend start",
    "install:all": "npm install && npm --prefix backend install"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
